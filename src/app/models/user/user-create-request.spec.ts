import { UserCreateRequest } from './user-create-request';

describe('UserCreateRequest', () => {
  it('should create an instance', () => {
    expect(new UserCreateRequest()).toBeTruthy();
  });
});
