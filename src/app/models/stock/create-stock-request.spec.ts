import { CreateStockRequest } from './create-stock-request';

describe('CreateStockForProductRequest', () => {
  it('should create an instance', () => {
    expect(new CreateStockRequest()).toBeTruthy();
  });
});
