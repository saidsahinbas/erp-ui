import { PredictionResponse } from './prediction-response';

describe('PredictionResponse', () => {
  it('should create an instance', () => {
    expect(new PredictionResponse()).toBeTruthy();
  });
});
