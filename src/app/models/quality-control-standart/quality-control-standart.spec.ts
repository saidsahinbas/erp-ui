import { QualityControlStandart } from './quality-control-standart';

describe('QualityControlStandart', () => {
  it('should create an instance', () => {
    expect(new QualityControlStandart()).toBeTruthy();
  });
});
