import { QualityControlResult } from './quality-control-result';

describe('QualityControlResult', () => {
  it('should create an instance', () => {
    expect(new QualityControlResult()).toBeTruthy();
  });
});
