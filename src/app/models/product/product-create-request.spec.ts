import { ProductCreateRequest } from './product-create-request';

describe('ProductCreateRequest', () => {
  it('should create an instance', () => {
    expect(new ProductCreateRequest()).toBeTruthy();
  });
});
