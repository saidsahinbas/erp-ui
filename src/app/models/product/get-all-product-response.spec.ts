import { GetAllProductResponse } from './get-all-product-response';

describe('GetAllProductResponse', () => {
  it('should create an instance', () => {
    expect(new GetAllProductResponse()).toBeTruthy();
  });
});
