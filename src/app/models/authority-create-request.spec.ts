import { AuthorityCreateRequest } from './authority-create-request';

describe('AuthorityCreateRequest', () => {
  it('should create an instance', () => {
    expect(new AuthorityCreateRequest()).toBeTruthy();
  });
});
