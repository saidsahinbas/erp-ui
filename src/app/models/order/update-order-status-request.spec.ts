import { UpdateOrderStatusRequest } from './update-order-status-request';

describe('UpdateOrderStatusRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateOrderStatusRequest()).toBeTruthy();
  });
});
