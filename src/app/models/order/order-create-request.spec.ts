import { OrderCreateRequest } from './order-create-request';

describe('OrderCreateRequest', () => {
  it('should create an instance', () => {
    expect(new OrderCreateRequest()).toBeTruthy();
  });
});
