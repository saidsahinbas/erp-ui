import { AuthorityGroupUpdateRequest } from './authority-group-update-request';

describe('AuthorityGroupUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new AuthorityGroupUpdateRequest()).toBeTruthy();
  });
});
