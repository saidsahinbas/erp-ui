import { UpdateCategoryRequestDto } from './update-category-request-dto';

describe('UpdateCategoryRequestDto', () => {
  it('should create an instance', () => {
    expect(new UpdateCategoryRequestDto()).toBeTruthy();
  });
});
